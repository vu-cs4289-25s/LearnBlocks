import { Card } from 'flowbite-react';

export default function ActivityWidget() {

  const activity = new Uint8Array([5])
  [{
   
  }]
  
  return (
    <Card className="flex flex-row">
      {}
    </Card>
  );
}
